# Relative SQLite paths resolve from prototype-api for both Prisma CLI and Nest.
# This is the default; copy to .env only when customizing local configuration.
DATABASE_URL="file:./prisma/dev.db"
PORT=3000
